class PopUp {
  String id;
  String classId;
  DateTime date;
  String title;
  int numRate;
  int sumRate;
  PopUp(
      this.id, this.classId, this.date, this.title, this.numRate, this.sumRate);
}
